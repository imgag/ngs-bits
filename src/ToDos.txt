=== TODO ===

libs:
- cppNGS:
   - VariantList:
      - Statistics: add test for statistics::somatic(...) (CS)
      - use QSharedPointers to link VariantAnnotationHeaders with VariantAnnotationDescriptions (CS)
      - check vcf and tsv files if valid (columns and their descriptions)
   - Statistics:
      - somatic(): add plot comparing depth tumor/normal and allele frequencies of dbSNP variants (CS)
      - somatic(): add cnv count - QCValue("somatic CNVs count", "", "Needs to be implemented.", "QC:2000044") (CS)
   - NGSHelper:
      - consider soft-clipping of secondary alignments (CS)

GSvar:
* Implement update tool that automatically checks for new versions, or find solution with reference to sub-folders (ask Martin)
- Allow handling of several affected (i.e. integrate SampleOverview functionality)
- Allow filtering of multi-sample variant lists => missing genotype (perhaps treat all variants as homozygous?)
- Add buttons to search in goolge/pubmed for variant
- Add MutationTaster link

Tools:
* MappingQC: support RNA: insert size, spike-in controls, mapping ribosomes/globulines, etc.
- SeqPurge:
  ? try using QBitArray instead of QByteArray to speed up sequence match counting - see test code at end of main.
  ? try pre-calculating all probabilities (bounded by factorial overflow) and test performance on large dataset.

CnvHunter:
 * update test data (include MAPQ=0, types: HBOC, TruRisk, Exome, Low-coverage genome data, Cancer data)
 * add optional GC-normalization (then, check GS151166_01 gene C5orf42) - test effect of including different flanking regions
 * test caclulate real ncov distribution also when enough data points are available. How many are that? Test/Simulate! (see line ~700: 'values.count()==n')
 * test mappability-normalization and exon-size normalization
 * test percentile normalization
 * test normalizing Z-scores to mean 0 and stdev 1 to correct for sample quality
 * test on HaloPlex HS data, multi-mapped data, chunked data
 * test using n>=10 and n<=30 with highest correlation or lowest z-score stdev instead of fixed 'n'
 - Für alle/wichtige CNAs Plot (log-ratio/z-score/call) mit Umgebung
 - integrate CNV databases:
   - http://dgv.tcag.ca/gb2/gbrowse/dgv2_hg19/
   - https://decipher.sanger.ac.uk/disorders#syndromes/overview
   - http://www.ncbi.nlm.nih.gov/dbvar
